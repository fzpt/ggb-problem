{
  "name": "geogebra-command-builder",
  "version": "1.0.0",
  "private": true,
  "description": "A GeoGebra Geometry pipeline: screenshot -> text -> commands -> interactive diagram.",
  "devDependencies": {
    "concurrently": "^8.2.2"
  },
  "scripts": {
    "dev": "concurrently \"node server.js\" \"cd client && npx vite --configLoader native\"",
    "dev:server": "node server.js",
    "start": "node server.js"
  },
  "dependencies": {
    "better-auth": "^1.7.2",
    "better-sqlite3": "^13.0.3",
    "express": "^5.2.1",
    "react": "^19.2.8",
    "react-dom": "^19.2.8"
  }
}
